# W7: the shield status HUD layer positions itself below the vanilla boss-bar
# stack, which requires the bar count (upstream: a Fabric mixin accessor on the
# same field). ModDevGradle picks this file up automatically at this path.
public net.minecraft.client.gui.components.BossHealthOverlay events # events

# W8: ScreenEffectManager applies the in-bubble screen post chain through the
# legacy loader, which 1.21.1 keeps private behind checkEntityPostEffect's
# hardcoded entity shaders (upstream 26.2 has a public setPostEffect; the
# Fabric source reached it via a mixin invoker). shutdownEffect/currentEffect
# are already public, so apply is the only slot access that needs opening.
public net.minecraft.client.renderer.GameRenderer loadEffect(Lnet/minecraft/resources/ResourceLocation;)V # loadEffect
