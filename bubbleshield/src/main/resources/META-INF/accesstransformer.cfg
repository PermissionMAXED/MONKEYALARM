# W7: the shield status HUD layer positions itself below the vanilla boss-bar
# stack, which requires the bar count (upstream: a Fabric mixin accessor on the
# same field). ModDevGradle picks this file up automatically at this path.
public net.minecraft.client.gui.components.BossHealthOverlay events # events
